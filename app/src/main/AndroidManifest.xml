<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.maidul.ecomercegarments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ItemReturn"></activity>
        <activity android:name=".activity.UserProfile" />
        <activity android:name=".activity.ProductAdd" />
        <activity android:name=".activity.Dashboard" />
        <activity android:name=".activity.Login" />
        <activity android:name=".activity.FlashPage3" />
        <activity android:name=".activity.FlashPage2" />
        <activity
            android:name=".activity.FlashPage1"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>